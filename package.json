{
  "name": "pixabay_project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../client && npm install",
    "start": "concurrently \"cd backend && npm start\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Elena",
  "license": "ISC"
}
